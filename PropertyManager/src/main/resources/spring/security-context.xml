<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
           http://www.springframework.org/schema/security
           http://www.springframework.org/schema/security/spring-security.xsd">

	<http auto-config="true" use-expressions="true">
		<intercept-url pattern="../../css" access="permitAll" />
		<intercept-url pattern="../../js" access="permitAll" />
		<intercept-url pattern="../../image" access="permitAll" />
		<intercept-url pattern="../../csv" access="permitAll" />
		<intercept-url pattern="/login.tmon" access="permitAll" />
		<intercept-url pattern="/*.tmon" access="hasAnyRole('ROLE_ADMIN','ROLE_USER')" />
		<form-login login-page="/login.tmon"
			authentication-failure-url="/login.tmon?is_fail=true"
			default-target-url="/showMembers.tmon" />
		<logout logout-success-url="/login.tmon" invalidate-session="true" />
	</http>

	<authentication-manager>
		<authentication-provider>
			<jdbc-user-service data-source-ref="dataSource"
				users-by-username-query="SELECT username, password FROM security WHERE username = ?"
				authorities-by-username-query="SELECT role as authority FROM security WHERE username = ?"
				/>
		</authentication-provider>
	</authentication-manager>
</beans:beans>